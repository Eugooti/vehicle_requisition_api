const intern={
    name: 'CRUD Methods',
    task: 'vehicle_allocation',
}

const ictOfficer = {
    ...intern,
    date: new Date(),
    deadline: "1 month"
}

console.log(ictOfficer)

console.log({intern:intern})